# ============================================================
# Kyrant Wears 2.0 — Environment Variables
# ============================================================
# Copy this file to .env and fill in your Supabase project values.
#
# Get these from: https://supabase.com/dashboard → Project Settings → API
#
# SECURITY RULES:
# - VITE_ prefixed vars are exposed to the browser (safe for anon key)
# - NEVER put the service_role key in a VITE_ variable
# - NEVER commit .env to version control
# ============================================================

# ── Supabase (Frontend-safe — exposed to browser) ──
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── Supabase (Server-side ONLY — NEVER prefix with VITE_) ──
# Only used in Node.js/Express backend or Supabase Edge Functions
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── Google OAuth (configured in Supabase Dashboard) ──
# No env var needed here — configured in:
# Supabase Dashboard → Authentication → Providers → Google
# You'll need:
#   - Google Cloud Console Client ID
#   - Google Cloud Console Client Secret
#   - Redirect URL: https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
